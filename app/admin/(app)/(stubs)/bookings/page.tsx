import { StubPage } from "../stub-page";

export const metadata = { title: "Bookings · Admin" };

export default function BookingsStub() {
  return (
    <StubPage
      title="Bookings"
      description="Recent contact-form submissions."
      hint="Coming in Phase 2 — list view, search, status, export."
    />
  );
}
