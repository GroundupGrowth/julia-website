// Marketing-site renderer for CMS-managed embeds.
//
// TODO (Phase 2): wire this up. Should fetch the embed row by `embedKey` and
// render its HTML via dangerouslySetInnerHTML if `enabled === true`. Until
// then this component returns null so importers don't error.
//
// Example usage once wired:
//   <Embed embedKey="booking-calendar" />

// eslint-disable-next-line no-unused-vars
export default function Embed({ embedKey }) {
  return null;
}
